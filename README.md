 # Le Etoile WIP Visualizer App

Add the firebase api key:

```
  firebase: {
      apiKey: 'your-key',
      databaseURL: 'your data base path'
     }
```

You can visit the app here: https://le-etoile.herokuapp.com/
